#include <stdio.h>
/**
 * main - Entry point of program
 * Return: 0
 */
int main(void)
{
	int c;

		for (c = 97; c <= 122; c++)
		{
			if ((c == 101) || (c == 113))

			{	continue;
			}
			putchar(c);

		}

			putchar(10);










return (0);
}
