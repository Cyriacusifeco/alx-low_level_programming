#include <stdio.h>
/**
 * main - Entry point of program
 * Return: 0
 */
int main(void)
{
	int c = 122;
		while (c <= 122 && c >= 97)
		{
			putchar(c);
			c--;
		}

			putchar(10);










return (0);
}
