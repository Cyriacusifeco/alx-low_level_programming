#include "main.h"
/* more headers goes there */

/**
 * factorial - Prints the factorial of an int
 * @n: int variable
 * Return: Always 1 on success
 */
int factorial(int n)
{
	int i = 0;

	if (s[i] != '\0')
	{
		return (1);
	}
		_putchar(s[i]);
			i++;

return (1);
}
