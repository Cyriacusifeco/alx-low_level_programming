#include "main.h"
/* more headers goes there */

/**
 * is_prime_number - Check if input is prime number
 * @n: Input variable
 * Return: Always 1 on success
 */
int is_prime_number(int n)
{
	int sqr;

	sqr = 2 * n;


return (sqr);
}
