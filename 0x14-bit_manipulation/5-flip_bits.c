#include <stdio.h>
#include <string.h>
#include <math.h>
#include "main.h"

/**
 * flip_bits(unsigned long int n, unsigned long int m) - flip bit
 * @n: Decimal number
 * @m: bit to flip
 * Return: binary
 */

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	m = 0;

		if (n < m)
			n = m + 1;
return (n);
}
